include:
  - .gitlab/k8s-development.yml

stages:
  - prebuild
  - build
  - release
  - deploy
  - test
  - sast
  - dast
  - setup

variables:
  npm_config_cache: "$CI_PROJECT_DIR/.npm"

.dependencies_cache:
  cache:
    key:
      files:
        - package-lock.json
    paths:
      - .npm
    policy: pull
